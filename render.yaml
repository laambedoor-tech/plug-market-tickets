services:
  - type: web
    name: plug-market-tickets
    env: node
    buildCommand: npm install && chmod +x start.sh
    startCommand: ./start.sh
    envVars:
      - key: NODE_ENV
        value: production